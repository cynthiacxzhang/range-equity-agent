{
  "name": "poker-agent",
  "version": "0.1.0",
  "description": "Range equity calculator for Texas Hold'em with Monte Carlo simulation",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
